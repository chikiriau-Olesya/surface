<%#= render "sidemenu" %>
<%= react_component 'M_AdminSidebar' %>

<%#= react_component 'M_CategoryMenu', { categories: @categories, category: @category } %>
<%= react_component 'O_AdminPosts', { posts: @posts, post: @post, categories: @categories, category: @category } %>

<%#= link_to 'Новая статья', newmagazine_posts_path %>


<!-- <table>
<%#= link_to(image_tag(@post.image.thumb.url, alt: 'Image'), @post.image.url, target: '_blank') if @post.image? %>
  <tbody>
    <%# @posts.each do |post| %>
      <tr>
				<td>
					 <%#= link_to(image_tag(post.image.thumb.url, alt: 'Image'), post.image.url, target: '_blank') if post.image? %>
				</td>
				 <td>
					 <%#= post.title %>
				 </td>
				 <td>
					 <%#= post.user.username %>
				</td>
				<!-- <td><#%= post.name %></td> -->
				<td>
				<%# if post.category_id? %>
					<span>
					<%#= post.category.name %>
					</span>
				<%# end %>
				</td>
        <td><%#= render_with_hashtags(post.content) %></td>
				<td><%#= post.post_type.name %></td>
        <td><%#= post.likes.count %> <%#= (post.likes.count) == 1 ? '💙' : '💙'%></td>
        <td><%#= post.coments.count %> <%#= (post.coments.count) == 1 ? ' 💬' : ' 💬'%></td>
        <td><%#= link_to '👀', post %></td>

				<!-- open view to delete and edit btn for admin in blog -->
				<%# if user_signed_in? && current_user.isadmin == true %>
					<td><%#= link_to '🛠', edit_post_path(post) %></td>
					<td><%#= link_to '🗑', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
				<%# end %>
      </tr>
    <%# end %>
  </tbody>
</table> -->
