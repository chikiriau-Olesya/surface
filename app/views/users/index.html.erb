<% if user_signed_in? && current_user.isadmin == true%>
  <%= react_component 'M_AdminNavbar' %>
<% end %>
<%= react_component 'O_AdminUsers', { users: @users } %>

<!-- <table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Username</th>
      <th>Email</th>
      <%# if user_signed_in? && current_user.isadmin == true %>
        <th>Admin</th>
        <th>Action</th>
      <%# end %>
    </tr>
  </thead>

  <tbody>
    <%# @users.each do |user| %>
      <tr>
        <td><%#= user.id %></td>
        <td><%#= user.username %></td>
        <td><%#= link_to user.email, user %></td>
        <td><%#= user.inst %></td>
        <td><%#= user.tg %></td>
        <td><%#= user.fb %></td>
        <td><%#= user.bio %></td>
        <%# if user_signed_in? && current_user.isadmin == true %>
          <td><%#= user.isadmin %></td>
        <%# end %>

        <!-- open view to delete users for admin-->
        <%# if user_signed_in? && current_user.isadmin == true %>
            <td><%#= link_to "Delete", user, method: :delete, data: { confirm: "Are you sure?" } %></td>
        <%# end %>
      </tr>
    <%# end %>
  </tbody>
</table> -->
