<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Username</th>
      <th>Email</th>
      <% if user_signed_in? && current_user.isadmin == true %>
        <th>Admin</th>
      <% end %>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.username %></td>
        <td><%= link_to user.email, user %></td>
        <% if user_signed_in? && current_user.isadmin == true %>
          <td><%= user.isadmin %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
