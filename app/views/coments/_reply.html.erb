<div class="card ml-5">
  <div class="card-body">
    <div>
      <% if reply.user_id %>
        <%= image_tag(reply.user.image.small_thumb.url, alt: 'Image') if reply.user.image? %>
      <% end %>
      <%= User.find(reply.user_id).username %>
    </div>
    <%= reply.body %>
    <%= link_to 'reply', new_post_coment_path(@post, parent_id: reply.id), remote: true, class: 'd-block' %>
    <% if user_signed_in? && current_user.id == reply.user_id  %>
      <p>
      <%# <%= link_to 'Destroy Comment', [reply.post, reply],
                 :confirm => 'Are you sure?',
                 :method => :delete %>  %>
       <%= link_to 'Destroy', reply, method: :delete, data: { confirm: 'Are you sure?' } %>
      </p>
    <% end %>
  </div>
</div>
<% if reply.replies.any? %>
  <% reply.replies.each do |reply| %>
    <%= render partial: 'coments/reply', locals: { reply: reply } %>
  <% end %>
<% end %>
<div id="reply-form-<%= reply.id %>"></div>
